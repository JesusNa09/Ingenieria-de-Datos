# PostgreSQL

## Aggregate Functions.
Entendi que las funciones de agregacion devulven un unico valor a partir de un conjunto de valores de entrada
y muestra la sintaxis de varias funciones en este caso las mas principales sum(), max(), min(), count(), avg()
su suntiaxis y con que tipo de valor se puede aplicar estas funciones.

## GROUP BY.
Viendo la documentacion vi que la clausula se usa para acomodar varios registros con el mismo tipo de valor de la o las columnas que especifiquemos
tambien nos mostro la sintaxis y algunos ejemplos de como usarla, ademas de que generalmente se usa cuando usamos las funciones de agregacion

## HAVING.
Aprendi que la funcion having trabaja con grupos de filas, este sintaxicamante siempre debe de ir despues de la clausula group by , ademas nos da algunos ejemplo para
ver como se debe declarar en tu consulta

## CASE.
Aprendi que case es una herramienta condicional booleana que devolvera verdadero en caso de que se cumpla la condicion o falso en caso de que no y continua evaluando
es como un if/else en programacion, ademas dio la sintaxis de como se debe hacer, evaluando cada una de las condiciones , devolvera el primer valor que encuentre como 
veradero y en caso de que no de ninguna condicion y no haya un else dara un valor nullo